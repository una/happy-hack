#!/usr/bin/env node
var debug = require('debug')('happy-hack');
var app = require('../app');

app.set('port', process.env.PORT || 3200);
var server = app.listen(app.get('port'), function() {
  debug('Express server listening at http://localhost:' + server.address().port);
});
